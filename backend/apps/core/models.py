"""
Core app models.

This app doesn't have database models - it only contains utilities and middleware.
"""

from django.db import models

# No models in core app - it's for utilities and middleware only
