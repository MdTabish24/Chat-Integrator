"""
Webhook models (if needed for logging/tracking).
For now, we'll use the webhook service for processing.
"""

from django.db import models

# No models needed for now - webhooks are processed in real-time
# We could add WebhookLog model later for debugging
