"""
Django admin configuration for webhooks app.
Webhooks app doesn't have models (processes in real-time).
"""

from django.contrib import admin

# No models to register - webhooks are processed in real-time
